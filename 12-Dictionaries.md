- To define a dictionary, use these symbols '{}'.
   ```python
      words = {key : value}
   ```

- Dictonaries are mutable.

- To add a unit to dictionaries.
   ```python
      words["x"] = "y"
   ```
